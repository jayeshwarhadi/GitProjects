#include <stdbool.h>
int findPosition(char piece);
bool verifyValidMove(char[],int from,int to);
bool isCheckmate();
bool isStalemate();
int Winner();
// logic.c file is hidden and will be proof of my original work